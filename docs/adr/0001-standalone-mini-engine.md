# Standalone Mini Engine

My Farm starts as a standalone project with its own small deterministic game core instead of depending on `farm-game-engine`. The core deliberately keeps compatible ideas such as commands, view DTOs, generated contracts, and integration-style tests so successful concepts can be upstreamed later without coupling this prototype to engine churn.

